# Configuración de Edge Functions
# verify_jwt=true por defecto: el gateway valida el JWT antes de llegar a la función.
# Las funciones create-user y delete-user además verifican el rol admin internamente.
