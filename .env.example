# Copia este archivo a `.env.local` y rellena con los valores de tu proyecto Supabase.
# NUNCA subas `.env`, `.env.local` ni archivos con secretos reales (están en .gitignore).
#
# Supabase → Project Settings → API

VITE_SUPABASE_URL=https://TU-REF.supabase.co
VITE_SUPABASE_ANON_KEY=TU_CLAVE_ANON_PUBLICA_JWT
