# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-legacy-win7
dist-ssr
*.local

# --- Secretos y entornos locales (NO subir a repos públicos) ---
# Credenciales reales: usa `.env.local` (ignorado). Plantilla versionada: `.env.example`.
.env
.env.*
!.env.example

# Claves y certificados (por si añades firma, push, etc.)
*.pem
*.key
*.p12
*.pfx

# Firebase / móvil (si los usas más adelante)
google-services.json
GoogleService-Info.plist

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Capacitor
# Descomenta las siguientes líneas si no quieres incluir ios/ y android/ en el repositorio
# ios/
# android/

# Electron
release/
release-win7/
dist-electron/
